#---------------------------------------------------------------------------
# *                            GitHub CLI
#---------------------------------------------------------------------------

if command_exists gh; then
    eval "$(gh copilot alias -- zsh)"
fi
